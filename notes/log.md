# Log

Append-only record of all operations.

- **2026-08-20**: Migrated the site to the [personal-notes-website-template](https://github.com/spatialthoughts/personal-notes-website-template) structure. Renamed `content/` to `notes/`, added this changelog, gave every topic page a Summary / Last-updated header, reformatted keyword tags from bracketed backtick lists to plain comma-separated `Keywords:` lines, and added `Related:` wiki-link backlinks between topic pages inferred from existing keywords. Switched the deploy workflow to GitHub Pages' Actions-based build and added the roamlinks plugin to resolve wiki-style links. No note content, links, or descriptions were rewritten.
- **2026-08-20**: Reinitialized the vault to fully match the template. Every existing note across all 26 topic pages now has a `Related:` link (previously only entries with an exact keyword-alias match got one) — derived from an expanded keyword vocabulary map plus a description-text fallback, with the ~40 remaining unmatched notes assigned by hand after reading each entry. Merged the external iCloud Obsidian vault into this repo: added `raw/` (with `raw/New_Notes.md` as the inbox) and `processed/` (gitignored), and a minimal `.obsidian/` config so the repo opens as a vault directly. Rewrote `CLAUDE.md` to closely mirror the template's section structure (Overview / Folder structure / Workflow / Processing Instructions / Update the Website / List of Topics / Topic Page Format / Rules) and now require a `Related:` link on every future note.
- **2026-08-20**: Restored mobile daily-note capture. Added a local, gitignored symlink `icloud-vault/` in this repo pointing at the iCloud-synced Obsidian vault (`~/Library/Mobile Documents/iCloud~md~obsidian/Documents/Notes`), so daily notes captured on mobile can be read and processed directly from this repo without manual copying, while topic pages stay tracked in `notes/`. Updated `CLAUDE.md`'s Overview/Folder structure/Workflow/Processing Instructions accordingly: daily notes are read from `icloud-vault/` and moved to `icloud-vault/processed/` (stays in iCloud, outside git) once ingested; `raw/` remains for notes dropped directly in this repo instead. Also gitignored Obsidian's local UI-state files (`.obsidian/workspace.json`).
- **2026-08-20**: Switched mobile capture from daily notes to a single running inbox. Cleaned up the iCloud-synced Obsidian vault down to just `raw/` (with `raw/New_Notes.md`) and `.obsidian/` — removed the 27 topic/index files (already migrated here), `processed/` (already logged here), `Daily Template.md`, and `Newsletter.md`. Everything removed was backed up first to `~/icloud-vault-backup-2026-08-20` (local, outside git). `Newsletter.md` contained one unprocessed resource link (a PySAL book) mixed in with a personal job-forward email — not auto-migrated since it wasn't a clean note; flagged for the vault owner instead. Repointed this repo's `raw/` from a real directory to a symlink straight at the iCloud vault's `raw/` folder, so mobile appends to `raw/New_Notes.md` and this repo see the exact same file — no separate daily-note flow anymore. `raw/` is now gitignored (it's a machine-specific symlink, like `icloud-vault/`).
- **2026-08-20**: Processed the first two notes through the new `raw/New_Notes.md` inbox. Added [Bhoonidhi MCP Server](https://geovicco-dev.github.io/bhoonidhi-mcp/) to `Claude_Code.md` (already tagged `[[Claude_Code]]` in the raw note) and [isobands: Xarray to GeoPandas Contours](https://palewi.re/docs/isobands/) to a new `## 2026` section in `XArray.md`. Updated `notes/index.md`'s Latest Finds with these two plus India Data Atlas (Data). Cleared `raw/New_Notes.md` back to empty.
- **2026-08-20**: Deduplicated Zone Builder (FOSS4G 2021) — it had been added to both `Papers.md` and `Urban_Planning.md` for the same URL. Removed the `Papers.md` copy (it's a tool, not a paper) and kept the `Urban_Planning.md` entry as the single copy, changing its `Related:` from `[[Python]]` to `[[Tools]]` per request.
- **2026-08-21**: Link-checked all ~303 unique URLs across every topic page. 13 dead links were fixed with working replacements (moved pages, renamed repos, or Wayback Machine snapshots where no live successor exists): `Agriculture.md` (PMFBY YESTECH manual → pmfby.gov.in), `Climate.md` (Copernicus CDS sea level dataset → new `/datasets/` URL), `Cartography.md` (Weaving Space FOSS4G slides → corrected `dosull.github.io/weavingspace` repo name), `Machine_Learning.md` (R-squared article → migrated to medium.com/data-science; Time Series Analysis Slides → Wayback snapshot), `QGIS.md` (QGIS for Crime Analysis → Wayback snapshot), `Python.md` (pyOpenSci Packaging Guide → renamed `/python-package-guide/` path), `STAC.md` (STOP and STAC series → Wayback snapshot; Carpentries STAC lesson → renumbered `05-access-data.html`; Sentinel-1 ARD STAC → moved to sentiwiki.copernicus.eu), `Misc.md` (Denmark AIS Data → dma.dk), `Water_Resources.md` (Groundwater Recharge GEE tutorial → Wayback snapshot), `Web_Mapping.md` (Cloud Native Geospatial Map → Wayback snapshot, site currently unreachable). 4 dead links had no viable replacement and were removed entirely: `GEE.md` (USDA GTAC digital soil mapping training doc, internal file server gone), `Misc.md` (TradingView personal watchlist; Visual Electric image generator — company was acquired by Perplexity and the product was shut down), `Machine_Learning.md` (Louis Quisset Labs geo embedding tool, domain expired with no archive).
